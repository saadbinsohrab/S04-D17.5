let numbers = [20, 65, 52, 66, 45, 55, 97, 50];

// how to know how many numbers have an array?-------------
console.log(numbers.length);

// how to check index number?------------------------------
console.log(numbers[2]);

// how to set an index number a different variable?--------
const num65 = numbers[1];
console.log(num65);

// how to change a value number of array?------------------
numbers[3] = 666;
console.log(numbers);
